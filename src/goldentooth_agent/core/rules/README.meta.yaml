module_name: Rules
description: Rules module
complexity: Medium
file_count: 3
loc: ~101
test_coverage: Medium
class_count: 2
function_count: 8
coverage_target: 90%+
test_perf: All tests <200ms
internal_dependencies: []
external_dependencies:
- collections
- dataclasses
- goldentooth_agent
- rule
- rule_engine
- typing
symbols:
- Rule
- RuleEngine
- TIn
- __all__
exports:
- Rule
- RuleEngine
classes:
- name: RuleEngine
  docstring: "A rule engine that evaluates a list of rules against a context and applies\
    \ actions based on matching conditions.\n\n    The RuleEngine is pure Flow-based\
    \ and processes rules in priority order. It can be converted\n    to a Flow for\
    \ stream processing or used directly for single context evaluation."
  methods:
  - name: evaluate
    signature: 'async def evaluate(self, ctx: TIn) -> TIn'
    docstring: Evaluate the rules against the context and apply actions for matching
      rules.
    first_line: Evaluate the rules against the context and apply actions for matching
      rules
    public: true
  - name: add_rule
    signature: 'def add_rule(self, rule: Rule[TIn]) -> None'
    docstring: Add a new rule to the rule engine.
    first_line: Add a new rule to the rule engine
    public: true
  - name: as_flow
    signature: def as_flow(self) -> Flow[TIn, TIn]
    docstring: Convert the rule engine to a flow that evaluates the rules for each
      item in a stream.
    first_line: Convert the rule engine to a flow that evaluates the rules for each
      item in a stream
    public: true
  public: true
- name: Rule
  docstring: "A rule that applies a condition to an input and executes an action if\
    \ the condition is met.\n\n    Rules are pure Flow-based components that evaluate\
    \ conditions and apply transformations\n    conditionally. They integrate seamlessly\
    \ with the Flow system for stream processing."
  methods:
  - name: apply
    signature: 'async def apply(self, ctx: TIn) -> TIn'
    docstring: Apply the rule to the given context.
    first_line: Apply the rule to the given context
    public: true
  - name: as_flow
    signature: def as_flow(self) -> Flow[TIn, TIn]
    docstring: Convert the rule to a flow that applies the rule to each item in a
      stream.
    first_line: Convert the rule to a flow that applies the rule to each item in a
      stream
    public: true
  public: true
