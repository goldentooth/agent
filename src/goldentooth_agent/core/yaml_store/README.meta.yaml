module_name: Yaml Store
description: Yaml Store module
complexity: Medium
file_count: 4
loc: ~78
test_coverage: Medium
class_count: 3
function_count: 10
coverage_target: 90%+
test_perf: All tests <200ms
internal_dependencies: []
external_dependencies:
- adapter
- base
- installer
- pathlib
- typing
- yaml
symbols:
- T
- YamlData
- YamlStore
- YamlStoreAdapter
- YamlStoreInstaller
- __all__
exports:
- YamlStore
- YamlStoreAdapter
- YamlStoreInstaller
classes:
- name: YamlStoreAdapter
  docstring: Protocol for classes that can be serialized to and from YAML.
  methods:
  - name: from_dict
    signature: 'def from_dict(cls, data: YamlData) -> T'
    docstring: Create an instance from a dictionary representation.
    first_line: Create an instance from a dictionary representation
    public: true
  - name: to_dict
    signature: 'def to_dict(cls, id: str, obj: T) -> YamlData'
    docstring: Convert the instance to a dictionary representation.
    first_line: Convert the instance to a dictionary representation
    public: true
  public: true
- name: YamlStoreInstaller
  docstring: A generic base class for installing YAML files from an embedded directory
    into a YAML store.
  methods:
  - name: install
    signature: 'def install(self, overwrite: bool) -> bool'
    docstring: Install all YAML files from the embedded directory into the store.
    first_line: Install all YAML files from the embedded directory into the store
    public: true
  public: true
- name: YamlStore
  docstring: A generic base class for managing YAML files in a directory.
  methods:
  - name: list
    signature: def list(self) -> list[str]
    docstring: List all available object names in the store.
    first_line: List all available object names in the store
    public: true
  - name: load
    signature: 'def load(self, id: str) -> T'
    docstring: Load an object by its ID from the YAML store.
    first_line: Load an object by its ID from the YAML store
    public: true
  - name: save
    signature: 'def save(self, id: str, obj: T) -> None'
    docstring: Save an object to the YAML store with the given ID.
    first_line: Save an object to the YAML store with the given ID
    public: true
  - name: delete
    signature: 'def delete(self, id: str) -> None'
    docstring: Delete an object by its ID from the YAML store.
    first_line: Delete an object by its ID from the YAML store
    public: true
  - name: exists
    signature: 'def exists(self, id: str) -> bool'
    docstring: Check if an object exists in the YAML store by its ID.
    first_line: Check if an object exists in the YAML store by its ID
    public: true
  public: true
constants:
- name: T
  value: ''
  public: true
- name: T
  value: ''
  public: true
- name: T
  value: ''
  public: true
