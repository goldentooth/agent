module_name: Embeddings
description: Embeddings module
complexity: Critical
file_count: 8
loc: ~3529
test_coverage: Medium
class_count: 9
function_count: 105
coverage_target: 90%+
test_perf: All tests <200ms
internal_dependencies: []
external_dependencies:
- anthropic
- antidote
- collections
- dataclasses
- datetime
- document_chunker
- embeddings_service
- gzip
- hashlib
- json
- math
- numpy
- openai
- openai_embeddings
- os
- pathlib
- paths
- re
- sqlite3
- sqlite_vec
- struct
- typing
- vector_store
- zlib
symbols:
- Chunk
- ChunkRelationshipAnalyzer
- DocumentChunk
- DocumentChunker
- EmbeddingsService
- HybridSearchEngine
- OpenAIEmbeddingsService
- SearchResult
- VectorStore
- __all__
exports:
- DocumentChunk
- DocumentChunker
- EmbeddingsService
- OpenAIEmbeddingsService
- VectorStore
classes:
- name: DocumentChunk
  docstring: Represents a chunk of a document with metadata.
  methods: []
  public: true
- name: DocumentChunker
  docstring: Intelligently splits documents into meaningful chunks for better RAG
    retrieval.
  methods:
  - name: chunk_document
    signature: 'def chunk_document(self, store_type: str, document_id: str, document_data:
      dict[str, Any]) -> list[DocumentChunk]'
    docstring: "Split a document into logical chunks based on its type and content.\n\
      \n        Args:\n            store_type: Type of document store (e.g., \"github.repos\"\
      , \"notes\")\n            document_id: Unique identifier for the document\n\
      \            document_data: Document content dictionary\n\n        Returns:\n\
      \            List of document chunks"
    first_line: Split a document into logical chunks based on its type and content
    public: true
  - name: get_chunk_summary
    signature: 'def get_chunk_summary(self, chunks: list[DocumentChunk]) -> dict[str,
      Any]'
    docstring: "Generate a summary of chunks for a document.\n\n        Args:\n  \
      \          chunks: List of document chunks\n\n        Returns:\n           \
      \ Dictionary with chunk summary information"
    first_line: Generate a summary of chunks for a document
    public: true
  public: true
- name: HybridSearchEngine
  docstring: Hybrid search engine combining semantic and keyword-based retrieval.
  methods:
  - name: hybrid_search
    signature: 'async def hybrid_search(self, query: str, max_results: int, store_type:
      str, include_chunks: bool, semantic_weight: float, keyword_weight: float, min_semantic_score:
      float, min_keyword_score: float, boost_exact_matches: bool, boost_title_matches:
      bool) -> list[dict[str, Any]]'
    docstring: "Perform hybrid search combining semantic and keyword scoring.\n\n\
      \        Args:\n            query: Search query string\n            max_results:\
      \ Maximum number of results to return\n            store_type: Optional store\
      \ type filter\n            include_chunks: Whether to include chunk results\n\
      \            semantic_weight: Weight for semantic similarity (0.0-1.0)\n   \
      \         keyword_weight: Weight for keyword scoring (0.0-1.0)\n           \
      \ min_semantic_score: Minimum semantic similarity threshold\n            min_keyword_score:\
      \ Minimum keyword score threshold\n            boost_exact_matches: Whether\
      \ to boost exact phrase matches\n            boost_title_matches: Whether to\
      \ boost title/metadata matches\n\n        Returns:\n            List of search\
      \ results with combined scores"
    first_line: Perform hybrid search combining semantic and keyword scoring
    public: true
  - name: get_search_stats
    signature: def get_search_stats(self) -> dict[str, Any]
    docstring: Get statistics about the search corpus.
    first_line: Get statistics about the search corpus
    public: true
  - name: update_hybrid_weights
    signature: 'def update_hybrid_weights(self, semantic_weight: float, keyword_weight:
      float) -> None'
    docstring: "Update the weights for hybrid scoring.\n\n        Args:\n        \
      \    semantic_weight: Weight for semantic similarity (0.0-1.0)\n           \
      \ keyword_weight: Weight for keyword scoring (0.0-1.0)"
    first_line: Update the weights for hybrid scoring
    public: true
  - name: update_bm25_parameters
    signature: 'def update_bm25_parameters(self, k1: float, b: float) -> None'
    docstring: "Update BM25 parameters.\n\n        Args:\n            k1: Term frequency\
      \ saturation parameter (default: 1.5)\n            b: Length normalization parameter\
      \ (default: 0.75)"
    first_line: Update BM25 parameters
    public: true
  - name: explain_search_results
    signature: 'async def explain_search_results(self, query: str, results: list[dict[str,
      Any]]) -> dict[str, Any]'
    docstring: "Provide detailed explanation of how search results were scored.\n\n\
      \        Args:\n            query: Original search query\n            results:\
      \ Search results with scoring breakdown\n\n        Returns:\n            Detailed\
      \ explanation of scoring methodology"
    first_line: Provide detailed explanation of how search results were scored
    public: true
  public: true
- name: Chunk
  docstring: Represents a chunk of content for embedding and search.
  methods: []
  public: true
- name: SearchResult
  docstring: Represents a search result with relevance scoring.
  methods:
  - name: chunk_id
    signature: def chunk_id(self) -> str
    docstring: Get chunk ID for convenience.
    first_line: Get chunk ID for convenience
    public: true
  - name: document_id
    signature: def document_id(self) -> str
    docstring: Get document ID for convenience.
    first_line: Get document ID for convenience
    public: true
  - name: content
    signature: def content(self) -> str
    docstring: Get content for convenience.
    first_line: Get content for convenience
    public: true
  public: true
- name: VectorStore
  docstring: Hybrid vector store supporting both SQLite and compressed sidecar files.
  methods:
  - name: store_document
    signature: 'def store_document(self, store_type: str, document_id: str, content:
      str, embedding: list[float], metadata: dict[str, Any] | None) -> str'
    docstring: "Store a document and its embedding.\n\n        Args:\n           \
      \ store_type: Type of document store (e.g., \"github.repos\")\n            document_id:\
      \ ID of the document\n            content: Text content that was embedded\n\
      \            embedding: Embedding vector\n            metadata: Additional metadata\n\
      \n        Returns:\n            Document database ID"
    first_line: Store a document and its embedding
    public: true
  - name: store_document_chunks
    signature: 'def store_document_chunks(self, store_type: str, document_id: str,
      chunks: list[Any], embeddings: list[list[float]], document_metadata: dict[str,
      Any] | None) -> list[str]'
    docstring: "Store document chunks and their embeddings.\n\n        Args:\n   \
      \         store_type: Type of document store (e.g., \"github.repos\")\n    \
      \        document_id: ID of the parent document\n            chunks: List of\
      \ DocumentChunk objects\n            embeddings: List of embedding vectors for\
      \ each chunk\n            document_metadata: Additional metadata for the parent\
      \ document\n\n        Returns:\n            List of chunk IDs that were stored"
    first_line: Store document chunks and their embeddings
    public: true
  - name: get_document_chunks
    signature: 'def get_document_chunks(self, store_type: str, document_id: str) ->
      list[dict[str, Any]]'
    docstring: "Get all chunks for a document.\n\n        Args:\n            store_type:\
      \ Type of document store\n            document_id: ID of the parent document\n\
      \n        Returns:\n            List of chunk metadata dictionaries"
    first_line: Get all chunks for a document
    public: true
  - name: delete_document_chunks
    signature: 'def delete_document_chunks(self, store_type: str, document_id: str)
      -> int'
    docstring: "Delete all chunks for a document.\n\n        Args:\n            store_type:\
      \ Type of document store\n            document_id: ID of the parent document\n\
      \n        Returns:\n            Number of chunks deleted"
    first_line: Delete all chunks for a document
    public: true
  - name: search_similar
    signature: 'def search_similar(self, query_embedding: list[float], limit: int,
      store_type: str | None, include_chunks: bool) -> list[dict[str, Any]]'
    docstring: "Search for documents similar to the query embedding.\n\n        Args:\n\
      \            query_embedding: Query vector\n            limit: Maximum number\
      \ of results\n            store_type: Optional filter by store type\n      \
      \      include_chunks: Whether to include chunks in search results\n\n     \
      \   Returns:\n            List of similar documents/chunks with metadata and\
      \ similarity scores"
    first_line: Search for documents similar to the query embedding
    public: true
  - name: get_document
    signature: 'def get_document(self, doc_id: str) -> dict[str, Any] | None'
    docstring: "Get a document by its ID.\n\n        Args:\n            doc_id: Document\
      \ database ID\n\n        Returns:\n            Document data or None if not\
      \ found"
    first_line: Get a document by its ID
    public: true
  - name: delete_document
    signature: 'def delete_document(self, doc_id: str) -> bool'
    docstring: "Delete a document and its embedding.\n\n        Args:\n          \
      \  doc_id: Document database ID\n\n        Returns:\n            True if document\
      \ was deleted, False if not found"
    first_line: Delete a document and its embedding
    public: true
  - name: list_documents
    signature: 'def list_documents(self, store_type: str | None, limit: int | None)
      -> list[dict[str, Any]]'
    docstring: "List all documents in the store.\n\n        Args:\n            store_type:\
      \ Optional filter by store type\n            limit: Optional limit on number\
      \ of results\n\n        Returns:\n            List of document metadata"
    first_line: List all documents in the store
    public: true
  - name: get_stats
    signature: def get_stats(self) -> dict[str, Any]
    docstring: "Get statistics about the vector store.\n\n        Returns:\n     \
      \       Dictionary with store statistics"
    first_line: Get statistics about the vector store
    public: true
  - name: get_all_sidecar_paths
    signature: def get_all_sidecar_paths(self) -> list[Path]
    docstring: "Get paths to all sidecar embedding files.\n\n        Returns:\n  \
      \          List of paths to .emb.gz files"
    first_line: Get paths to all sidecar embedding files
    public: true
  - name: count_sidecar_files
    signature: def count_sidecar_files(self) -> int
    docstring: "Count the number of sidecar embedding files.\n\n        Returns:\n\
      \            Number of .emb.gz files"
    first_line: Count the number of sidecar embedding files
    public: true
  - name: sync_sidecar_files
    signature: def sync_sidecar_files(self) -> dict[str, Any]
    docstring: "Sync all embeddings to sidecar files.\n\n        Returns:\n      \
      \      Dictionary with sync results"
    first_line: Sync all embeddings to sidecar files
    public: true
  - name: get_sidecar_metadata
    signature: def get_sidecar_metadata(self) -> dict[str, Any]
    docstring: "Get metadata about sidecar files.\n\n        Returns:\n          \
      \  Metadata dictionary"
    first_line: Get metadata about sidecar files
    public: true
  - name: store_chunk_relationships
    signature: 'def store_chunk_relationships(self, relationships: list[dict[str,
      Any]]) -> int'
    docstring: "Store chunk relationships in the database.\n\n        Args:\n    \
      \        relationships: List of relationship dictionaries with keys:\n     \
      \           - source_chunk_id: ID of source chunk\n                - target_chunk_id:\
      \ ID of target chunk\n                - relationship_type: Type of relationship\n\
      \                - strength: Relationship strength (0.0-1.0)\n             \
      \   - strength_category: Optional category (weak/moderate/strong)\n        \
      \        - metadata: Optional metadata dict\n\n        Returns:\n          \
      \  Number of relationships stored"
    first_line: Store chunk relationships in the database
    public: true
  - name: get_chunk_relationships
    signature: 'def get_chunk_relationships(self, chunk_id: str, relationship_types:
      list[str], min_strength: float, limit: int) -> list[dict[str, Any]]'
    docstring: "Get chunk relationships from the database.\n\n        Args:\n    \
      \        chunk_id: Optional chunk ID to filter relationships for\n         \
      \   relationship_types: Optional list of relationship types to filter\n    \
      \        min_strength: Minimum relationship strength to include\n          \
      \  limit: Optional limit on number of results\n\n        Returns:\n        \
      \    List of relationship dictionaries"
    first_line: Get chunk relationships from the database
    public: true
  - name: get_related_chunks
    signature: 'def get_related_chunks(self, chunk_id: str, max_related: int, min_strength:
      float, relationship_types: list[str]) -> list[dict[str, Any]]'
    docstring: "Get chunks related to a specific chunk.\n\n        Args:\n       \
      \     chunk_id: ID of the chunk to find relationships for\n            max_related:\
      \ Maximum number of related chunks to return\n            min_strength: Minimum\
      \ relationship strength to include\n            relationship_types: Optional\
      \ list of relationship types to filter\n\n        Returns:\n            List\
      \ of related chunks with relationship metadata"
    first_line: Get chunks related to a specific chunk
    public: true
  - name: delete_chunk_relationships
    signature: 'def delete_chunk_relationships(self, chunk_id: str, relationship_type:
      str) -> int'
    docstring: "Delete chunk relationships.\n\n        Args:\n            chunk_id:\
      \ Optional chunk ID to delete relationships for\n            relationship_type:\
      \ Optional relationship type to delete\n\n        Returns:\n            Number\
      \ of relationships deleted"
    first_line: Delete chunk relationships
    public: true
  - name: get_relationship_stats
    signature: def get_relationship_stats(self) -> dict[str, Any]
    docstring: "Get statistics about chunk relationships.\n\n        Returns:\n  \
      \          Dictionary with relationship statistics"
    first_line: Get statistics about chunk relationships
    public: true
  - name: analyze_chunk_network
    signature: def analyze_chunk_network(self) -> dict[str, Any]
    docstring: "Analyze the chunk relationship network structure.\n\n        Returns:\n\
      \            Dictionary with network analysis results"
    first_line: Analyze the chunk relationship network structure
    public: true
  public: true
- name: EmbeddingsService
  docstring: Service for creating and managing document embeddings using Anthropic.
  methods:
  - name: create_embedding
    signature: 'async def create_embedding(self, text: str) -> list[float]'
    docstring: "Create an embedding for the given text.\n\n        Args:\n       \
      \     text: Text content to embed\n\n        Returns:\n            List of embedding\
      \ values (vector)\n\n        Raises:\n            ValueError: If embedding creation\
      \ fails"
    first_line: Create an embedding for the given text
    public: true
  - name: create_document_embedding
    signature: 'async def create_document_embedding(self, document_data: dict[str,
      Any]) -> dict[str, Any]'
    docstring: "Create an embedding for a structured document.\n\n        Args:\n\
      \            document_data: Document data dictionary from YAML\n\n        Returns:\n\
      \            Dictionary with embedding and metadata"
    first_line: Create an embedding for a structured document
    public: true
  - name: embed_batch
    signature: 'async def embed_batch(self, texts: list[str]) -> list[list[float]]'
    docstring: "Create embeddings for a batch of texts.\n\n        Args:\n       \
      \     texts: List of text strings to embed\n\n        Returns:\n           \
      \ List of embedding vectors"
    first_line: Create embeddings for a batch of texts
    public: true
  - name: create_document_chunks_with_embeddings
    signature: 'async def create_document_chunks_with_embeddings(self, store_type:
      str, document_id: str, document_data: dict[str, Any]) -> dict[str, Any]'
    docstring: "Create chunks for a document and generate embeddings for each chunk.\n\
      \n        Args:\n            store_type: Type of document store (e.g., \"github.repos\"\
      )\n            document_id: ID of the document\n            document_data: Document\
      \ content dictionary\n\n        Returns:\n            Dictionary with chunks,\
      \ embeddings, and metadata"
    first_line: Create chunks for a document and generate embeddings for each chunk
    public: true
  - name: create_chunk_embedding
    signature: 'async def create_chunk_embedding(self, chunk: DocumentChunk) -> dict[str,
      Any]'
    docstring: "Create an embedding for a single document chunk.\n\n        Args:\n\
      \            chunk: Document chunk to embed\n\n        Returns:\n          \
      \  Dictionary with embedding and metadata"
    first_line: Create an embedding for a single document chunk
    public: true
  - name: get_embeddable_text_from_chunk
    signature: 'def get_embeddable_text_from_chunk(self, chunk: DocumentChunk) ->
      str'
    docstring: "Extract embeddable text from a document chunk.\n\n        This is\
      \ primarily the chunk content, but could be enhanced\n        to include contextual\
      \ information.\n\n        Args:\n            chunk: Document chunk\n\n     \
      \   Returns:\n            Text content suitable for embedding"
    first_line: Extract embeddable text from a document chunk
    public: true
  - name: re_embed_document_with_chunks
    signature: 'async def re_embed_document_with_chunks(self, store_type: str, document_id:
      str, document_data: dict[str, Any]) -> dict[str, Any]'
    docstring: "Re-embed a document using chunking.\n\n        Args:\n           \
      \ store_type: Type of document store\n            document_id: ID of the document\n\
      \            document_data: Document content dictionary\n\n        Returns:\n\
      \            Dictionary with chunks, embeddings, and metadata"
    first_line: Re-embed a document using chunking
    public: true
  - name: should_use_chunking
    signature: 'def should_use_chunking(self, store_type: str, document_data: dict[str,
      Any]) -> bool'
    docstring: "Determine if a document should be chunked based on type and size.\n\
      \n        Args:\n            store_type: Type of document store\n          \
      \  document_data: Document content dictionary\n\n        Returns:\n        \
      \    True if document should be chunked, False otherwise"
    first_line: Determine if a document should be chunked based on type and size
    public: true
  public: true
- name: ChunkRelationshipAnalyzer
  docstring: Analyzes semantic relationships between document chunks.
  methods:
  - name: analyze_chunk_relationships
    signature: 'async def analyze_chunk_relationships(self, chunks: list[DocumentChunk],
      chunk_embeddings: list[list[float]], include_cross_document: bool) -> dict[str,
      Any]'
    docstring: "Analyze relationships between chunks.\n\n        Args:\n         \
      \   chunks: List of document chunks to analyze\n            chunk_embeddings:\
      \ Corresponding embeddings for chunks\n            include_cross_document: Whether\
      \ to analyze relationships across documents\n\n        Returns:\n          \
      \  Dictionary containing relationship analysis results"
    first_line: Analyze relationships between chunks
    public: true
  - name: find_related_chunks
    signature: 'async def find_related_chunks(self, target_chunk_id: str, relationship_data:
      dict[str, Any], max_related: int, min_strength: float) -> list[dict[str, Any]]'
    docstring: "Find chunks related to a target chunk.\n\n        Args:\n        \
      \    target_chunk_id: ID of chunk to find relationships for\n            relationship_data:\
      \ Output from analyze_chunk_relationships\n            max_related: Maximum\
      \ number of related chunks to return\n            min_strength: Minimum relationship\
      \ strength to include\n\n        Returns:\n            List of related chunks\
      \ with relationship metadata"
    first_line: Find chunks related to a target chunk
    public: true
  - name: get_chunk_context_expansion
    signature: 'def get_chunk_context_expansion(self, chunk_ids: list[str], relationship_data:
      dict[str, Any], expansion_radius: int) -> set[str]'
    docstring: "Expand a set of chunks to include related chunks.\n\n        Args:\n\
      \            chunk_ids: Initial set of chunk IDs\n            relationship_data:\
      \ Relationship analysis data\n            expansion_radius: How many relationship\
      \ hops to include\n\n        Returns:\n            Expanded set of chunk IDs\
      \ including related chunks"
    first_line: Expand a set of chunks to include related chunks
    public: true
  public: true
- name: OpenAIEmbeddingsService
  docstring: Service for creating and managing document embeddings using OpenAI.
  methods:
  - name: create_embedding
    signature: 'async def create_embedding(self, text: str) -> list[float]'
    docstring: "Create an embedding for the given text using OpenAI.\n\n        Args:\n\
      \            text: Text content to embed\n\n        Returns:\n            List\
      \ of embedding values (vector)\n\n        Raises:\n            ValueError: If\
      \ embedding creation fails"
    first_line: Create an embedding for the given text using OpenAI
    public: true
  - name: create_document_embedding
    signature: 'async def create_document_embedding(self, document_data: dict[str,
      Any]) -> dict[str, Any]'
    docstring: "Create an embedding for a structured document.\n\n        Args:\n\
      \            document_data: Document data dictionary from YAML\n\n        Returns:\n\
      \            Dictionary with embedding and metadata"
    first_line: Create an embedding for a structured document
    public: true
  - name: embed_batch
    signature: 'async def embed_batch(self, texts: list[str], batch_size: int) ->
      list[list[float]]'
    docstring: "Create embeddings for a batch of texts efficiently.\n\n        Args:\n\
      \            texts: List of text strings to embed\n            batch_size: Maximum\
      \ number of texts to embed in one API call\n\n        Returns:\n           \
      \ List of embedding vectors"
    first_line: Create embeddings for a batch of texts efficiently
    public: true
  - name: create_document_chunks_with_embeddings
    signature: 'async def create_document_chunks_with_embeddings(self, store_type:
      str, document_id: str, document_data: dict[str, Any]) -> dict[str, Any]'
    docstring: "Create chunks for a document and generate embeddings for each chunk.\n\
      \n        Args:\n            store_type: Type of document store (e.g., \"github.repos\"\
      )\n            document_id: ID of the document\n            document_data: Document\
      \ content dictionary\n\n        Returns:\n            Dictionary with chunks,\
      \ embeddings, and metadata"
    first_line: Create chunks for a document and generate embeddings for each chunk
    public: true
  - name: create_chunk_embedding
    signature: 'async def create_chunk_embedding(self, chunk: DocumentChunk) -> dict[str,
      Any]'
    docstring: "Create an embedding for a single document chunk.\n\n        Args:\n\
      \            chunk: Document chunk to embed\n\n        Returns:\n          \
      \  Dictionary with embedding and metadata"
    first_line: Create an embedding for a single document chunk
    public: true
  - name: get_embeddable_text_from_chunk
    signature: 'def get_embeddable_text_from_chunk(self, chunk: DocumentChunk) ->
      str'
    docstring: "Extract embeddable text from a document chunk.\n\n        Args:\n\
      \            chunk: Document chunk\n\n        Returns:\n            Text content\
      \ suitable for embedding"
    first_line: Extract embeddable text from a document chunk
    public: true
  - name: re_embed_document_with_chunks
    signature: 'async def re_embed_document_with_chunks(self, store_type: str, document_id:
      str, document_data: dict[str, Any]) -> dict[str, Any]'
    docstring: "Re-embed a document using chunking.\n\n        Args:\n           \
      \ store_type: Type of document store\n            document_id: ID of the document\n\
      \            document_data: Document content dictionary\n\n        Returns:\n\
      \            Dictionary with chunks, embeddings, and metadata"
    first_line: Re-embed a document using chunking
    public: true
  - name: should_use_chunking
    signature: 'def should_use_chunking(self, store_type: str, document_data: dict[str,
      Any]) -> bool'
    docstring: "Determine if a document should be chunked based on type and size.\n\
      \n        Args:\n            store_type: Type of document store\n          \
      \  document_data: Document content dictionary\n\n        Returns:\n        \
      \    True if document should be chunked, False otherwise"
    first_line: Determine if a document should be chunked based on type and size
    public: true
  - name: get_model_info
    signature: def get_model_info(self) -> dict[str, Any]
    docstring: "Get information about the current embedding model.\n\n        Returns:\n\
      \            Dictionary with model information"
    first_line: Get information about the current embedding model
    public: true
  public: true
