module_name: Registry
description: Registry module
complexity: Low
file_count: 2
loc: ~259
test_coverage: Medium
class_count: 1
function_count: 15
coverage_target: 90%+
test_perf: All tests <200ms
internal_dependencies: []
external_dependencies:
- collections
- core
- main
- typing
symbols:
- AnyFlow
- FlowInfo
- FlowRegistry
- Input
- Output
- __all__
- flow_registry
- get_flow
- list_flows
- register_flow
- registered_flow
- search_flows
exports:
- FlowRegistry
- flow_registry
- get_flow
- list_flows
- register_flow
- registered_flow
- search_flows
classes:
- name: FlowRegistry
  docstring: Registry for named flows with discovery and introspection capabilities.
  methods:
  - name: register
    signature: 'def register(self, name: str, flow: AnyFlow, category: str | None)
      -> AnyFlow'
    docstring: "Register a flow with the given name.\n\n        Args:\n          \
      \  name: Unique name for the flow\n            flow: Flow instance to register\n\
      \            category: Optional category for organization\n\n        Returns:\n\
      \            The registered flow (for chaining)"
    first_line: Register a flow with the given name
    public: true
  - name: get
    signature: 'def get(self, name: str) -> AnyFlow | None'
    docstring: "Get a flow by name.\n\n        Args:\n            name: Name of the\
      \ flow to retrieve\n\n        Returns:\n            Flow instance or None if\
      \ not found"
    first_line: Get a flow by name
    public: true
  - name: list_flows
    signature: 'def list_flows(self, category: str | None) -> list[str]'
    docstring: "List all registered flow names.\n\n        Args:\n            category:\
      \ Optional category to filter by\n\n        Returns:\n            List of flow\
      \ names"
    first_line: List all registered flow names
    public: true
  - name: list_categories
    signature: def list_categories(self) -> list[str]
    docstring: "List all categories.\n\n        Returns:\n            List of category\
      \ names"
    first_line: List all categories
    public: true
  - name: search
    signature: 'def search(self, query: str) -> list[str]'
    docstring: "Search for flows by name or metadata.\n\n        Args:\n         \
      \   query: Search query (substring match)\n\n        Returns:\n            List\
      \ of matching flow names"
    first_line: Search for flows by name or metadata
    public: true
  - name: remove
    signature: 'def remove(self, name: str) -> bool'
    docstring: "Remove a flow from the registry.\n\n        Args:\n            name:\
      \ Name of the flow to remove\n\n        Returns:\n            True if removed,\
      \ False if not found"
    first_line: Remove a flow from the registry
    public: true
  - name: clear
    signature: 'def clear(self, category: str | None) -> None'
    docstring: "Clear flows from registry.\n\n        Args:\n            category:\
      \ Optional category to clear, or None to clear all"
    first_line: Clear flows from registry
    public: true
  - name: info
    signature: 'def info(self, name: str) -> FlowInfo | None'
    docstring: "Get detailed information about a flow.\n\n        Args:\n        \
      \    name: Name of the flow\n\n        Returns:\n            Dictionary with\
      \ flow information or None if not found"
    first_line: Get detailed information about a flow
    public: true
  - name: print_registry
    signature: def print_registry(self) -> None
    docstring: Print a formatted view of the registry.
    first_line: Print a formatted view of the registry
    public: true
  public: true
functions:
- name: register_flow
  signature: 'def register_flow(name: str, flow: AnyFlow, category: str | None) ->
    AnyFlow'
  docstring: "Register a flow in the global registry.\n\n    Args:\n        name:\
    \ Unique name for the flow\n        flow: Flow instance to register\n        category:\
    \ Optional category for organization\n\n    Returns:\n        The registered flow\
    \ (for chaining)"
  public: true
- name: get_flow
  signature: 'def get_flow(name: str) -> AnyFlow | None'
  docstring: "Get a flow from the global registry.\n\n    Args:\n        name: Name\
    \ of the flow to retrieve\n\n    Returns:\n        Flow instance or None if not\
    \ found"
  public: true
- name: list_flows
  signature: 'def list_flows(category: str | None) -> list[str]'
  docstring: "List flows in the global registry.\n\n    Args:\n        category: Optional\
    \ category to filter by\n\n    Returns:\n        List of flow names"
  public: true
- name: search_flows
  signature: 'def search_flows(query: str) -> list[str]'
  docstring: "Search flows in the global registry.\n\n    Args:\n        query: Search\
    \ query\n\n    Returns:\n        List of matching flow names"
  public: true
- name: registered_flow
  signature: 'def registered_flow(name: str, category: str | None) -> Callable[[Any],
    Any]'
  docstring: "Decorator to register a flow in the global registry.\n\n    Args:\n\
    \        name: Unique name for the flow\n        category: Optional category for\
    \ organization\n\n    Example:\n        @registered_flow(\"text_processor\", \"\
    nlp\")\n        @Flow.from_sync_fn\n        def process_text(text):\n        \
    \    return text.upper()\n\n        Or with factory functions:\n        @registered_flow(\"\
    my_flow\")\n        def create_flow():\n            return map_stream(lambda x:\
    \ x + 1)"
  public: true
