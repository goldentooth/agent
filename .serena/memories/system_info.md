# System Information

## Development Environment
- **Operating System**: macOS (Darwin)
- **Python Version**: 3.11 (specified in .python-version)
- **Git**: Available for version control

## macOS-Specific Commands
```bash
# File operations
ls -la              # List files with details
find . -name "*.py" # Find files
grep -r "pattern"   # Search recursively

# System info
uname -a           # System information
which python       # Find Python executable
python --version   # Python version
```

## Git Repository
- Repository is initialized with git
- Standard .gitignore for Python projects
- Uses main branch
- No CI/CD or GitHub Actions configured

## Development Tools
- No IDE-specific configuration files present
- No editor configuration (.editorconfig) found
- Serena agent configured for code assistance
- Claude Code assistant instructions in CLAUDE.md