# Suggested Commands

## Running the Application
```bash
python main.py
```

## Development Commands
Since this is a minimal Python project with no dependencies or build tools configured yet:

### Basic Python Commands
```bash
# Run the main application
python main.py

# Check Python version
python --version

# Install packages (when needed)
pip install package_name
```

### Git Commands (macOS/Darwin)
```bash
git status
git add .
git commit -m "message"
git push
```

### System Commands (macOS/Darwin)
```bash
ls -la          # List files
find . -name "*.py"  # Find Python files
grep -r "pattern" .  # Search for patterns
```

## Notes
- No testing framework is set up yet
- No linting or formatting tools configured
- No package manager (like uv, poetry, pipenv) in use
- Project uses standard Python packaging with pyproject.toml
